#include<bits/stdc++.h>
using namespace std;
bool check(vector<int>&a,vector<int>&c)
int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        vector<int>a(n),c(n);
        for(auto &x : a) cin>>x;
        for(auto &y : c) cin>>y;
        //find the max number of cookiees
        int low = 0,high = 1e9;
        while(high<=low){
            int mid = low + (high-low)/2;

        }
    }
}